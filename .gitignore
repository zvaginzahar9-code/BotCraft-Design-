node_modules
.next
.verify
next-env.d.ts
tsconfig.tsbuildinfo

# Остатки прежней файловой базы: приложение работает с PostgreSQL и в data/ не пишет
data/*.db
data/*.db-shm
data/*.db-wal

# Исходники ассетов: оригиналы весят ~450 МБ и служат только входом для
# конвейера в scripts/. В репозиторий едет оптимизированный результат в public/.
assets/raw

# Локальные переменные окружения
.env
.env.local
.env*.local

# Служебные файлы ОС и редакторов
.DS_Store
Thumbs.db
*.log

# Локальные рабочие материалы, не входящие в проект: исходные модели,
# черновые скриншоты, видеопитч и служебные скрипты разработчика.
/дополнительные модельки/
/до.после/
/video-pitch/
/РЕФЕРЕНС ДЛЯ СПИЧ ДИНАМИКА/
.dev-shot.mjs
.ed2.mjs
